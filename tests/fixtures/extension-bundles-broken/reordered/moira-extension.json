{
  "apiVersion": "moira.extensions/v1",
  "name": "reordered",
  "version": "1.0.0",
  "entrypoint": "index.ts",
  "nodes": [
    {
      "type": "reordered.go",
      "title": "Reordered",
      "configSchema": { "type": "object", "additionalProperties": false },
      "outputSchema": { "type": "object", "required": ["observed"] }
    }
  ]
}
